export default class UI {
    constructor(xPos, yPos, canvas){
        this.PosX = xPos;
        this.PosY = yPos;
        this.canvas = canvas;
        this.fontSize = 30;
        console.log("UI has been instantiated.")
    }
}